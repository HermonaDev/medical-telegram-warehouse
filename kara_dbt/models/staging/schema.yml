version: 2

models:
  - name: stg_telegram_data
    columns:
      - name: message_id
        tests:
          - not_null
      - name: date_key
        tests:
          - not_null

  - name: dim_dates
    columns:
      - name: date_key
        tests:
          - unique
          - not_null